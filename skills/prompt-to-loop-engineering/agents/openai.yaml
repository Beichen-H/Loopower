interface:
  display_name: "Prompt to Loop Engineering"
  short_description: "Build validated Codex loop-agent scaffolds"
  default_prompt: "Use $prompt-to-loop-engineering to build and statically validate a KB v4.0.2 loop_design_result and, when requested, a lightweight .codex-loop scaffold without installing a Runtime Engine."

policy:
  allow_implicit_invocation: true
