interface:
  display_name: "Prompt to Loop Engineering"
  short_description: "Build verified evidence-locked loop agents"
  default_prompt: "Use $prompt-to-loop-engineering v3 to normalize this request, derive the smallest evidence-justified registry of dynamic professional roles, statically validate the loop_design_result, and when approved persist a lightweight .codex-loop scaffold whose LoopSpec controls transitions and termination, whose Codex host enforces policy, whose reviewers emit evidence only, and which adds no Runtime Engine."

policy:
  allow_implicit_invocation: true
