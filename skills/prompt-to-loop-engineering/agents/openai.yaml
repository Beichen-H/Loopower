interface:
  display_name: "Prompt to Loop Engineering"
  short_description: "Build verified evidence-locked loop agents"
  default_prompt: "Use $prompt-to-loop-engineering v2 to normalize this request with verified provenance, build and statically validate a KB v4.0.2 loop_design_result, and when requested persist a lightweight .codex-loop scaffold with access-mode-isolated reviewers, versioned progress evidence, inherited subagent reasoning intensity, and no Runtime Engine."

policy:
  allow_implicit_invocation: true
